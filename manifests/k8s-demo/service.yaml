apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo
  labels:
    app: demo-app
spec:
  clusterIP: None
  selector:
    app: demo-app
  ports:
    - name: http
      port: 80
      targetPort: http
